import { Routes, Route } from "react-router-dom";

export const Router = () => {
  return (
    <Routes>
      <Route path="/" />
      <Route path="/login" />
      <Route path="/singup" />
    </Routes>
  );
};
